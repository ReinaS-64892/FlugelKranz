# FlugelKranz

Reina_Sakiria が実現した VRChat の「自由飛行」を、Monado / WiVRn の空間操作 API で発展させるためのツールです。アバターギミックではありません。

## 起動

.NET 10 SDK、Linux の Wayland セッション、Monado / WiVRn のサービスと対応する OpenXR ランタイムが必要です。

```sh
dotnet restore FlugelKranz.slnx
dotnet build FlugelKranz.slnx
dotnet run --project src/FlugelKranz
```

libmonado は `XR_RUNTIME_JSON` または XDG の OpenXR runtime manifest（`~/.config/openxr/1/active_runtime.json` など）にある `MND_libmonado_path` から自動検出します。検出できない場合の既定パスは `/usr/lib/wivrn/libmonado_wivrn.so` です。`--lib-monado PATH` で明示指定できます。

起動時の引数処理は `System.CommandLine` を使用します。`--help` でオプションを確認できます。

```sh
dotnet run --project src/FlugelKranz -- --lib-monado /path/to/libmonado.so
```

OpenXR ローダー（`libopenxr_loader.so.1`）が同じ Monado / WiVRn サービスを使用するよう設定してください。必要に応じて `XR_RUNTIME_JSON=/path/to/runtime.json` を起動時に指定します。FlugelKranz はシステムのランタイム設定を変更しません。

OpenXR は NuGet の [Evergine.Bindings.OpenXR](https://github.com/EvergineTeam/OpenXR.NET) を使用します。

## 操作

操作はコントローラーごとの論理 Drag / Turn に割り当てます。ON にした後やモード変更・位置姿勢リセットの後は、操作入力を一度離してから掴み直してください。

- **Valve Index**: 左手トラックパッド右／右手トラックパッド左の内側入力が Drag、左手トラックパッド左／右手トラックパッド右の外側入力が Turn です。OpenXR からトラックパッドの touch・座標・force を読み、中央の位置デッドゾーン外かつ感圧閾値以上を D-pad として判定します。既定値は位置デッドゾーン `0.30`、感圧閾値 `0.20` で、Valve Index 専用設定から `0.00`～`1.00` の範囲で変更できます。両手のトラックパッド下を同時に 1 秒押すとモードを切り替えます。
- **Oculus Touch 系**: Thumb Rest に触れ、同時にトリガーへ指が触れていれば Drag、触れていなければ Turn です。
- UI の丸い `I` / `F` ボタンでも、無限歩行（Infinite Walking）と自由飛行（Free Flight）を切り替えられます。

libmonado が見つかった起動時は、既定の無限歩行モードで自動的に ON になります。見つからない場合は OFF で起動し、ON にした時点で接続エラーを UI に表示します。

無限歩行から自由飛行への切替は現在の位置・姿勢を保ったまま即時に行います。自由飛行から無限歩行へ切り替えると、1 秒かけて飛行差分の高さを `0`（接続時の高さ）へ滑らかに戻し、現在の Y 軸回転を残してピッチとロールを水平へ戻します。移行中は Drag / Turn を受け付けず、完了後に掴み直します。

### 無限歩行モード

Space Drag は物理空間の Y 軸だけを直接移動し、慣性を使いません。両手で Drag すると両手位置の中点を使い、移動量を 2 倍にします。Space Turn は Y 軸回転だけで、片手・両手を問わず常に頭を支点とします。片手 Turn はその手の Yaw、両手 Turn は HMD の Yaw を逆回転として空間へ反映し、頭の向きを打ち消します。この支点は自由飛行モードの Turn 原点設定から独立しています。片手・両手が切り替わるたびに現在位置で掴み直すため、切替フレームでは飛びや加速を発生させません。

無限歩行専用の **ドラグスムーズ**は Drag、Turn、両手で頭を支点にする Turn Head のいずれも `0.0` 秒が既定値です。各値は `0.0`～`1.0` 秒で調整し、`0.0` 秒では補間せず直接反映します。

**無限歩行ブースト**は Turn による回転中に生じた HMD の水平移動を進行方向へ追加し、足が後ろへ滑る感覚を補正します。補正値は追加する移動量の倍率で、既定値は `0.6`、範囲は `0.0`～`2.0` です。`0.0` では無効、`1.0` では計測した水平移動と同じ距離を追加します。回転していないフレーム、Turn 開始・片手／両手切替・HMD トラッキング復帰の基準化フレームでは追加しません。

### 自由飛行モード

片手 Drag はそのコントローラー位置、両手 Drag は両手位置の中点を掴みます。片手 Turn は全軸回転です。既定の Turn 原点は、有効に追跡されている HMD・左右コントローラー位置の中点で、設定から頭原点へ変更できます。Drag と Turn が同時なら、Turn 原点より Drag の掴み位置を優先します。

両手 Turn は二つのコントローラーを結ぶ直線を軸とする回転だけを反映します。二手目の Drag / Turn が加わったときと、両手 Turn から片手へ戻ったときは、その場で開始位置・姿勢を再計算し、切替そのものから慣性を生成しません。両手 Drag は片方を離した時点で残る手の Drag も論理的に解除し、その手を一度離して掴み直すまで再開しません。片手 Drag と片手 Turn の混在は、それぞれの通常動作を同時に行います。

操作を離すと、その時点の移動速度・全軸角速度を引き継いで慣性移動します。自由飛行モードで使う慣性パラメーターは次のとおりです。

- **慣性カットオフ**は解放速度が Drag `40 cm/s`、Turn `45 °/s` 未満なら慣性を開始しません。開始済みの慣性が減速によって閾値を下回っても停止には使用しません。チェックを外すと無効になります。
- **慣性加速倍率**は解放速度を Drag・Turn 別に `0.0`～`5.0` 倍します。既定値は Drag `1.0`、Turn `0.4` です。
- **Z加速**はプレイヤーの正面を `Z+` として、Drag 解放時の前後成分だけを追加倍率で強めます。方向との内積の絶対値を重みとするため、横方向には追加加速をかけません。範囲は `1.0`～`5.0`、既定値は `2.0` です。
- **慣性加速ブーストモード**を有効にすると、Drag 中のコントローラー移動速度がカットオフを超えている間は Drag ブレーキをかけず、解放時の加速を残っている慣性へ加算します。コントローラーが再び低速または停止するとブレーキも再開します。加算後の速度は、残っている慣性と今回の解放加速の大きい方を基準に最大倍率で制限され、既定値は有効・`4.0` 倍（`1.0`～`6.0`）です。
- **ベクトル回転倍率**は Turn で現在の移動慣性ベクトルを回す割合です。既定値 `1.0` では Turn に完全追従して曲線軌道を作り、`0.0` では回転前の進行方向を維持します。
- **慣性減速**は毎秒の指数減衰率です。`0.0` は減速なし、`10.0` は急速な減速で、既定値は `2.0 /秒` です。
- **慣性減速免除**の時間は、現在速度から実用上の停止速度まで通常減速する予測時間に対する割合です。Drag と Turn を個別に設定でき、既定値は Drag `0.20`、Turn `0.15` です。免除割合の既定値は共通の `0.90` です。
- **ドラグスムーズ**は空間オフセットへ反映する Drag と Turn の追従だけを、それぞれ `0.0`～`1.0` 秒の時定数で平滑化します。既定値は Drag `0.01` 秒、Turn `0.05` 秒です。慣性速度は平滑化前のコントローラー操作から計算します。
- **ドラグブレーキ**は新しく操作した瞬間に対応する慣性を完全に減衰させます。Drag は線速度だけ、Turn は角速度だけに作用します。適用時間を `0.0`～`1.0` 秒で設定でき、既定値は `0.40` 秒、`0.0` 秒なら即時停止します。発動時は対応する慣性減速免除を解除します。

「OFF」は入力と慣性を止め、位置・姿勢を保持します。「接続時の位置・姿勢に戻す」は ON 状態を維持したまま初期オフセットへ復元し、操作状態をクリアします。通常のウィンドウ終了時も復元します。強制終了やサービス切断では復元できない場合があります。他のツールがオフセットを変更した場合は停止し、その変更を上書きしません。HMD のトラッキングを失った間は操作と慣性を一時停止し、オン状態・操作状態・速度を保持して復帰時に再開します。トラッキングを失った手は対象操作から外し、残る手があればその場で掴み直します。

## 対応条件と制約

- libmonado API **1.4 以降の 1.x**。公開 API のみを使用し、Monado サブモジュールは変更しません。
- OpenXR の `XR_MND_headless` と `XR_KHR_convert_timespec_time`、STAGE / VIEW 空間が必要です。描画なしセッションを使い、VRChat からプライマリー・フォーカスを奪う操作は行いません。
- HMD・左右コントローラーは別々のトラッキング原点でも使用できます。各原点のオフセットを読み取り、飛行の変換は `mnd_root_set_reference_space_offset` で STAGE 参照空間へ一度だけ適用します。トラッキング原点自体は書き換えないため、スペースキャリブレーターや HMD 再接続による原点更新と共存できます。固定 STAGE オフセットを libmonado から読める構成が必要です。ドライバーが動的に提供する STAGE は起動時に理由を表示して操作を開始しません。
- 入力割当は Valve Index と Oculus Touch 系に対応します。他のコントローラーへグリップや select の代替割当は行いません。
- STAGE 参照空間のオフセットは、その空間を使う他の XR クライアントにも作用します。HMD・左右手が共有する原点を使うフルボディトラッカーなども同じ変換を受けます。専用の別原点は変更しません。
- VRChat 内の移動・水平線調整はアプリ側の座標変換です。本ツールはその設定を読み書きしません。

## 検証

```sh
dotnet test FlugelKranz.slnx
dotnet run --project src/FlugelKranz -- --diagnose --lib-monado /path/to/libmonado.so
```

`--diagnose` は UI を開かず、接続先の一致・基準空間・HMD と両手の入力を確認します。検出した各トラッキング原点について、接続時と現在のオフセットも表示します。参照空間オフセットは書き込みません。接続できても有効な入力が揃わなければ非ゼロで終了します。

自動テストは論理入力割当、二手目での再基準化、両手 Drag / Turn、無限歩行の軸制約、自由飛行の原点と慣性、既存オフセット、トラッキング喪失、オン・オフ・復元、UI と設定移行を対象にします。実機では VRChat を動かしたまま、Index / Touch の入力、片手・両手の切替、両モード、各回転軸、操作解放、オフ、復元、終了を確認してください。自動テストだけではランタイム・コントローラー・VRChat の組み合わせを保証できません。

## 座標変換

HMD・左手・右手の物理トラッキング原点から Monado ルートへの変換を、それぞれ `Tₕ`、`Tₗ`、`Tᵣ`、STAGE からルートへの変換を `S` とします。`Tᵢ⁰` は接続時の原点オフセット、`Tᵢ` は現在のオフセットです。各 OpenXR 姿勢 `Pᵢ` は `Tₕ⁰⁻¹ × Tᵢ⁰ × Tᵢ⁻¹ × S × Pᵢ` として **HMD の接続時の物理座標系**へ復元します。HMD 自身では中間項が相殺されます。積は右側を先に適用します。原点の更新は毎フレーム読み取り、変換の書き込み先にはしません。

HMD の操作結果を `Tₕ'` とすると、飛行の差分は `D = Tₕ' × Tₕ⁻¹` です。OpenXR の姿勢は STAGE からの逆変換で報告されるため、libmonado の STAGE オフセットは `S' = D⁻¹ × S` として更新します。これにより Index・VIVE Tracker・Quest のように初期キャリブレーションが異なる原点も、同じ参照空間の移動を受けます。`mnd_root_set_reference_space_offset` は OpenXR の参照空間変更イベントを発生させるため、入力クライアントは既存の空間ハンドルを保持したままイベントを処理します。原点更新や再接続で `Tᵢ` が変わっても、読み取った値で物理座標への変換を継続します。

自由飛行の Drag は選択した手、または両手の物理座標の中点を固定します。Turn は開始時の選択手姿勢に現在姿勢の逆回転を合成し、片手では全軸、両手ではコントローラー間の軸へ拘束します。並進は有効な追跡要素の中点、頭、または同時 Drag の固定点が動かないように補正し、Drag と Turn を動かし続けても固定点を維持します。無限歩行では同じ物理座標から Y 並進だけを抽出し、Turn は片手のコントローラーまたは両手時の HMD から Y 回転だけを抽出します。VRChat 世界と飛行者の軸への対応は、VRChat 側の水平線調整などを合成した結果になります。
